[package]
cairo-version = "2.6.3"
name = "dojo_phaser"
version = "0.0.1"

[cairo]
sierra-replace-ids = true

[scripts]
migrate = "sozo build && sozo migrate apply && ./scripts/init.sh"


[dependencies]
dojo = { git = "https://github.com/dojoengine/dojo", tag = "v1.0.0-alpha.12" }

[[target.dojo]]